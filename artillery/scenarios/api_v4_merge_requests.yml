config:
  http:
    pool: 10

  defaults:
    headers:
      Content-Type: "application/json"

  phases:
    - duration: 2
      arrivalRate: 2
      rampTo: 20
      name: "Warm up"

    - duration: 10
      arrivalRate: 20
      name: "Load"

scenarios:
  - flow:
      - get:
          url: /api/v4/projects/{{ PROJECT_ID }}/merge_requests
          expect:
            - statusCode: 200
